<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 1. Exterior Shadow -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <solid android:color="#80000000" />
        </shape>
    </item>

    <!-- 2. Main Button Body -->
    <item android:bottom="2dp" android:right="2dp">
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <gradient
                android:startColor="@color/bg_card"
                android:centerColor="@color/bg_surface"
                android:endColor="@color/bg_primary"
                android:angle="270" />
            <stroke android:width="1dp" android:color="@color/bg_card_pressed" />
        </shape>
    </item>

    <!-- 3. Top-left Highlight for 3D depth -->
    <item android:bottom="3dp" android:right="3dp" android:left="1dp" android:top="1dp">
        <shape android:shape="rectangle">
            <corners android:radius="15dp" />
            <stroke android:width="1dp" android:color="#30FFFFFF" />
        </shape>
    </item>

    <!-- 4. Subtle sheen -->
    <item android:bottom="2dp" android:right="2dp">
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <gradient
                android:gradientRadius="200dp"
                android:startColor="#0AFFFFFF"
                android:endColor="#00FFFFFF"
                android:type="radial"
                android:centerX="0.3"
                android:centerY="0.2" />
        </shape>
    </item>
</layer-list>
