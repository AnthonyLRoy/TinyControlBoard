<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- BLE permissions for Android < 12 (API < 31) -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <!-- Location required for BLE scanning on Android 6-11 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- BLE permissions for Android 12+ (API 31+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- Album art is fetched over plain HTTP from moOde on the home LAN -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.TinyRemote">

        <activity
            android:name=".ui.ScanActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.ViewSelectionActivity"
            android:exported="false"
            android:label="@string/view_selection_title"
            android:parentActivityName=".ui.MainActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.LibraryActivity"
            android:exported="false"
            android:label="@string/library_title"
            android:parentActivityName=".ui.MainActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.SearchResultsActivity"
            android:exported="false"
            android:label="@string/search_results_title"
            android:parentActivityName=".ui.LibraryActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.PlaylistActivity"
            android:exported="false"
            android:label="@string/playlist_title"
            android:parentActivityName=".ui.MainActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.PlaylistManagementActivity"
            android:exported="false"
            android:label="@string/playlist_management_title"
            android:parentActivityName=".ui.PlaylistActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.PlaylistListActivity"
            android:exported="false"
            android:parentActivityName=".ui.PlaylistManagementActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>
